<form method="post" action="{{ path('back_establishment_delete', {'id': establishment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ establishment.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
