{% extends 'back/menu.html.twig' %}

{% block title %}{{ district.name }}{% endblock %}

{% block body_back %}

    <h1>{{ district.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ district.name }}</td>
        </tbody>
    </table>

    <a href="{{ path('back_district_index') }}" class="btn btn-primary">back to list</a>
    
    <a href="{{ path('back_district_edit', {'id': district.id}) }}" class="btn btn-success">edit</a>

    {{ include('back/district/_delete_form.html.twig') }}

{% endblock %}
