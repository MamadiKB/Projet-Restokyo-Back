{% extends 'base.html.twig' %}

{% block title %}Modifier un district{% endblock %}

{% block body_back %}
    <h1>Modifier un district</h1>

    {{ include('district/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_district_index') }}" class="btn btn-info">Retour à la liste des districts</a>

    {{ include('district/_delete_form.html.twig') }}
{% endblock %}
