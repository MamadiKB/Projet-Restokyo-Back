{% extends 'base.html.twig' %}

{% block title %}{{user.pseudo}}{% endblock %}

{% block body_back %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    
    <a class="btn btn-info btn-m" href="{{ path('back_user_index') }}">back to list</a>

    <a class="btn btn-success btn-m" href="{{ path('back_user_edit', {'id': user.id}) }}">edit</a>   

    {{ include('user/_delete_form.html.twig') }}
    
    
{% endblock %}
